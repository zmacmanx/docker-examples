ApiVersion: v1
kind: pod
metadata:
  name: myapp-pod
  labels:
    app: myapp
spec:
   containers:
     - name: mapp-containers
       image: busybox
       command: [ 'sh', '-c', 'echo hello kube && sleep 10' ]